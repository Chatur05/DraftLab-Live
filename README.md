# DraftLab Live

This repository hosts the public runnable build of DraftLab 2026.

The source project remains private. `index.html` is automatically synchronized from `Draftlab_2026.html` in the private source repository whenever that file changes.
